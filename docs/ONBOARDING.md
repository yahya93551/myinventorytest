# Onboarding Checklist

1. Read `README.md` and `CONTRIBUTING.md`.
2. Copy `env.example` to `.env.local` and get secrets from the team.
3. Install dependencies: `npm install`.
4. Run dev server: `npm run dev`.
5. Run `npm run typecheck` and `npm run lint`.
6. Pick a small issue and open a draft PR.
